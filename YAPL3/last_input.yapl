class Main {
    booleano: Bool <- true;
  	io: IO <- (new IO);
    age: Int <- 0;
    name: String <- " ";

    main():Int{
        name <- "Luis";
        io.outString(name);
        io.outString(" ingrese su edad: ");
        age <- (io.inInt());

        if age>17 then
            (io.outString("\nEs mayor de edad"))
        else
            (io.outString("\nEs menor de edad"))
        fi;
        return 1;
    }

}

(new Main).main();
