class Rectangle{
    length: Int <- 5;
    width: Int <- 10;
    newbool: Bool <- 1;

    operations(): Int{
        length <- (10 + 4);
        width <- (5/2);
        length <- (10 - 10);
        length <- (10 * 7);
        return 1;
    }
}

class Main{

    main():Int{
        return 1;
    }
}
(new Main).main();